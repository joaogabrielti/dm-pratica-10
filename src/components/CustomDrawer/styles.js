import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        marginTop: 8,
        marginBottom: 16,
        paddingStart: 8,
        paddingEnd: 8,
        alignItems: 'center',
    },
    text: {
        color: '#6E5494',
        fontSize: 18,
        fontWeight: '700',
        marginStart: 8,
        marginEnd: 'auto',
    },
})

export default styles