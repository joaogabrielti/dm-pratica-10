import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    center: {
        flex: 1,
        marginTop: 8,
        alignItems: 'center',
    },
    button: {
        paddingStart: 8,
        paddingEnd: 8,
        width: '100%',
    },
    items: {
        marginTop: 16,
    },
})

export default styles